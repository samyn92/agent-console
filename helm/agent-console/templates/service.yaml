apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-console.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-console.selectorLabels" . | nindent 4 }}
